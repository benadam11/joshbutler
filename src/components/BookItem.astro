---
const { id, title, description, postImage, options } = Astro.props;
---

<section class="grid gap-10">
  <h4 id={id}>{title}</h4>
  <a href={`/books/${id}`}>
    <figure>
      <img src={postImage} />
      <figcaption class="mt-4">Learn More</figcaption>
    </figure>
  </a>
  <!-- <p>{description}</p> -->
</section>
